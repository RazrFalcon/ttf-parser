[package]
name = "ttf-parser"
version = "0.1.0"
authors = ["Evgeniy Reizner <razrfalcon@gmail.com>"]
keywords = ["ttf", "truetype", "opentype"]
categories = ["parser-implementations"]
license = "MIT/Apache-2.0"
description = "A high-level, safe, zero-allocation TrueType font parser."
repository = "https://github.com/RazrFalcon/ttf-parser"
documentation = "https://docs.rs/ttf-parser/"
readme = "README.md"
edition = "2018"

[dev-dependencies]
bencher = "0.1"
svgtypes = { version = "0.4.4", default-features = false }
time = "0.1"

[features]
default = ["std"]
std = []

[[bench]]
name = "outline"
harness = false

[package.metadata.docs.rs]
features = ["std"]
